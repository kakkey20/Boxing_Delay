Boxについてバチコリ書かれている

標準環境の使用:
gymのCartPole-v1環境を作成し、観測空間（env.observation_space）とアクション空間（env.action_space）を調査。
環境をリセットし、ランダムなアクションを選択してステップを実行し、観測、報酬、終了状態などの出力を表示。

カスタム環境の定義と検証:
GoLeftEnvというカスタム環境を定義。この環境は、エージェントが常に左に移動することを学ぶ必要がある1Dグリッド環境です。
gym.Envを継承し、reset、step、render、closeメソッドを実装。
stable_baselines3.common.env_checkerを使用して、カスタム環境がgymインターフェースに適合していることを確認。


カスタム環境でのエージェントの手動テスト:
GoLeftEnv環境をインスタンス化し、複数のステップでエージェントを手動で左に移動させ、結果を表示。

RLモデルのトレーニングとテスト:
stable_baselines3ライブラリのA2Cアルゴリズムを使用して、カスタム環境でモデルをトレーニング。
ベクトル化された環境（vec_env）でトレーニングされたモデルをテストし、予測されたアクションと結果を表示。
